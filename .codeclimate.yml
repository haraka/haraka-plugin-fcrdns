engines:
  eslint:
    enabled: true
    channel: "eslint-8"
    config:
      config: ".eslintrc.yaml"

ratings:
   paths:
   - "index.js"

checks:
  return-statements:
    config:
      threshold: 6
